cmake_minimum_required(VERSION 3.0)
PROJECT(track)
SET(SRC_LIST track.cpp)
find_package(OpenCV 3.4 REQUIRED)
include_directories(${OpenCV_INCLUDE_DIRS})
ADD_EXECUTABLE(demo ${SRC_LIST})
target_link_libraries(demo ${OpenCV_LIBS})
