cmake_minimum_required(VERSION 3.5)
PROJECT(load)
SET(SRC_LIST load.cpp)
find_package(OpenCV 3.4 REQUIRED)
include_directories(${OpenCV_INCLUDE_DIRS})
ADD_EXECUTABLE(show ${SRC_LIST})
target_link_libraries(show ${OpenCV_LIBS})
